#include <stdio.h>

int main()
{
    // int a = 10;
    // int b = 5;

    /*
        In the c programming there is 3 types of operator
            1. Arithmetic ( + - * / % )
            2. Conditional ( < <= > >= != == )
            3. Logical ( && || ! )
    */


    return 0;
}