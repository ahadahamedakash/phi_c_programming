#include <stdio.h>
#include <string.h>

int main()
{
    // int n = 1000001;

    // char arr[n];

    // scanf("%s", arr);

    // int length = strlen(arr);

    // int sum = 0;

    // for (int i = 0; i < length; i++)
    // {
    //     int digit = arr[i] - '0';
    //     sum += digit;
    // }

    // printf("%d", sum);

    char str[20] = "Hello World";
    printf("%s", str);

    return 0;
}
