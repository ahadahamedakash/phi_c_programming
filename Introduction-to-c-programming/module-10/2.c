#include <stdio.h>

int main()
{
    char cArr[10];

    scanf("%s", &cArr);

    // PRINT FULL CHARACTER ARRAY IN SINGLE LINE
    printf("%s\n", cArr);

    // PRINT SINGLE CHARACTER FROM ARRAY
    printf("%d\n", cArr[5]);

    return 0;
}